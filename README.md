# Unix profiles
Profiles for linux/macos environments: bash, vim, tmux, etc

# Copy-pase for quick install

ln -sf .bash_profile ~/.
ln -sf .bashrc ~/.
ln -sf .gitconfig ~/.
ln -sf .tmux.conf ~/.
ln -sf .vimrc ~/.
